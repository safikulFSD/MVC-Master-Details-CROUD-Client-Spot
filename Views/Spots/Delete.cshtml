@model work_01.Models.Spot

@{
    ViewBag.Title = "Delete";
}



<h3 class="display-4 alert alert-danger">Are you sure you want to delete this spot?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SpotName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SpotName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a href="@Url.Action("Index","Spots")" class="btn btn-link btn-sm" style="text-decoration:none;"><i class="fa fa-arrow-circle-left"></i> Back to list</a>
        </div>
    }
</div>
